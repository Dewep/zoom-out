export default store => {
  // called when the store is initialized
  // let resizeDispatch = null

  // window.addEventListener('resize', function (event) {
  //   if (resizeDispatch) {
  //     clearTimeout(resizeDispatch)
  //   }
  //   resizeDispatch = setTimeout(() => {
  //     store.dispatch('updateResize', { width: window.innerWidth, height: window.innerHeight })
  //     resizeDispatch = null
  //   }, 50)
  // })
}
